<app title="Braced Record" version="0.1">

    <client>

        {currentId = ""}

        <editor type="layout" orient="vertical" table="notes" record="{app.currentId}">
            <titleInput type="text-input" field=".title"/>
        </editor>

    </client>

</app>
